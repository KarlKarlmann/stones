org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
# Hier die Version eintragen (wird fuer die .jar, mods.toml und den Git-Commit genutzt)
mod_version=1.0.3

# Mixins
mixin_version=0.8.5

# JEI Integration (Versionen für build.gradle)
jei_mc_version=1.20.1
jei_version=15.3.0.4